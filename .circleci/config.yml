version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: build container
          command: make container
          shell: /bin/bash
      - run:
          name: push container
          command: make container-push-travis
          shell: /bin/bash
